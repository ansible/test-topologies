apiVersion: v1
kind: ServiceAccount
metadata:
  annotations:
    eks.amazonaws.com/role-arn: "${AWS_STORAGE_ROLE}"
  labels:
    app.kubernetes.io/name: automation-hub
    app.kubernetes.io/part-of: automation-hub
  name: automation-hub
  namespace: ansible-automation-platform
