apiVersion: external-secrets.io/v1beta1
kind: ClusterExternalSecret
metadata:
  name: automation-hub-postgres-configuration
  namespace: external-secrets
spec: 
  externalSecretName: "automation-hub-postgres-configuration"  
  namespaceSelector:
    matchLabels: 
      kubernetes.io/metadata.name: ansible-automation-platform
  refreshTime: 1m
  externalSecretSpec:
    refreshInterval: 1m
    secretStoreRef:
      name: cluster-secret-store
      kind: ClusterSecretStore
    target:
      name: automation-hub-postgres-configuration
      template:
        type: Opaque
        metadata:
          labels:
            app.kubernetes.io/part-of: "automation-hub"
        data:
          host: "{{ .host }}"
          username: "{{ .username }}"
          password: "{{ .password }}"
          database: automationhub
          port: '5432'
          type: unmanaged
    data:
    - secretKey: host
      remoteRef:
        key: "${CLUSTER_NAME}"
        property: DATABASE_HOST
    - secretKey: password
      remoteRef:
        key: "${CLUSTER_NAME}"
        property: DATABASE_HUB_PASSWORD
    - secretKey: username
      remoteRef:
        key: "${CLUSTER_NAME}"
        property: DATABASE_HUB_USERNAME